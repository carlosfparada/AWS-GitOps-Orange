#junos-config
groups {
    aws-default {
        system {
            host-name HOSTNAME;
            services {
                ssh {
                    root-login deny-password;
                }
            }
            login {
                user ec2-user {
                    uid 100;
                    class super-user;
                    authentication {
                        ssh-rsa "SSHRSAKEY";
                    }
                }
            }
            syslog {
                user * {
                    any emergency;
                }
                file messages {
                    any notice;
                    authorization info;
                }
                file interactive-commands {
                    interactive-commands any;
                }
            }
        }
        interfaces {
            fxp0 {
                unit 0 {
                    family inet {
                        address FXP0ADDR;
                    }
                }
            }
        }
        routing-options {
            static {
                route 0.0.0.0/0 {
                    next-hop GATEWAY;
                    retain;
                    no-readvertise;
                }
            }
        }
    }
}
apply-groups aws-default;