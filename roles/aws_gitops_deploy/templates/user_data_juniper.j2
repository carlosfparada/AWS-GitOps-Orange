#junos-config
groups {
    global {
        system {
            host-name Juniper-1;
            services {
                ssh {
                    protocol-version v2;
                    root-login allow;
                }
            }
            login {
                user ec2-user {
                    class super-user;
                    authentication {
                        plain-text-password "Admin";
                    }
                }
            }
            authentication-order password;
        }
    }
}
apply-groups global;